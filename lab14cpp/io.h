#include <vector>
#include <string>

std::vector<int> inputFromConsole();
std::vector<int> inputFromFile(const std::string& filename);
std::vector<int> inputRandom(int n);
std::vector<int> getVector();
void printVector(std::vector<int>& V);